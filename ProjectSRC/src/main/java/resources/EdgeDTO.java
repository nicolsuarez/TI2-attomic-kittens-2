package resources;

public class EdgeDTO {
    // Just using the name as a String to be searched on the map with the values,
    // so it saves the same object with the same memory reference
    public String from;
    public String to;
    public int weight;
}
