package resources;

import graphStructures.WeightedGraph;
import model.Room;

public class BuildMap {

    public static WeightedGraph<Room> loadGraphFromJSON(String fileName) {
        WeightedGraph<Room> graph = new WeightedGraph<>();

        return graph;
    };
}
